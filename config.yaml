
asr:
  model: small
  device: auto
  beam_size: 5
  vad_filter: false
  language: ja

subtitles:
  max_line_len: 22
  min_duration: 0.8

study:
  enabled: true

paths:
  output_dir: ./output

volumes:
  - ./config.yaml:/app/config.yaml:ro